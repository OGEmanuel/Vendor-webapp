import SectionCard from '@/ui/TUI/Components/SectionCard';
import { Box } from '@mantine/core';

export default function Settings() {
  return (
    <SectionCard caption="Change account settings" title="Settings">
      <Box></Box>
    </SectionCard>
  );
}
